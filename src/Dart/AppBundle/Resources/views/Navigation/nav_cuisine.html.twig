{% block cuisine_nav %}
    {% set short = dropdown is defined ? dropdown : false %}
    {% set template = 'AppBundle:Navigation:nav_cuisine_menu.html.twig' %}
    
    {% if short %}
        {% set template = 'AppBundle:Navigation:nav_cuisine_dropdown.html.twig' %}
    {% endif %}
    
    {{ include(template, {'cuisines': cuisines}) }}
{% endblock %}