{% block cuisine_menu %}
    <ul class="dropdown-menu">
        {% for cuisine in cuisines %}
        <li><a href="{{ path('app_cuisine', {'id': cuisine.id}) }}">{{ cuisine.name }}</a></li>
        {% endfor %}
    </ul>
{% endblock %}